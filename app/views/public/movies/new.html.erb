<%= render 'errors', movie: @movie %>
<h1>新規投稿</h1>
  <%= form_with model: @movie, url:public_movies_path do |f| %>
    <h4>動画</h4>
      <%= f.file_field :movie %>
    <h4>説明</h4>
      <%= f.text_area :content %>
    <%= f.submit '投稿' %>
  <% end %>