<footer class="bg-dark text-white pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-4">
            <p>ゴルフ上達SNS</p>
          </div>
          <div class="col-md-8">
            <h6 class="text-white mt-2 mb-3">
              <% if customer_signed_in? %>
               <%= link_to "退会する", public_unsubscribe_path(current_customer.id) %>
              <% end %>
            </h6>

          </div>
        </div>
      </div>
    </footer>