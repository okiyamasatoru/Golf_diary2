<div>
  <h3><%= @customer.name %></h3>
  <%= image_tag @customer.get_profile_image(80,80) %>
  <%= link_to "編集", edit_admin_customer_path(@customer.id) %>
</div>

<!-- ユーザーの投稿一覧 -->
<% @movies.each do |movie| %>
<div>
  <%= link_to admin_movie_path(movie.id) do %>
    <video src="<%= movie %>" type="video/mp4" controls></video>
  <% end %>
　<p>content：<%= movie.content %></p>
　<p><%= movie.created_at %></p>
　<p><%= link_to "#{movie.comments.count} コメント", admin_movie_path(movie.id) %></p>
</div>
<% end %>